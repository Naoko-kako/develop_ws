#include <stdio.h>

int main(){
    int d;
    printf("input number: ");
    scanf("%d",&d);
    if(d == 0){

    }
    else {
        printf("input number: ");
        scanf("%d",&d);
    }
    return 0;
}